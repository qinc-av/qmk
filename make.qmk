#!/bin/sh
__shebang_exec_hack=0 exec make -f ${0} ${@}
QMK?=$(dir $(realpath $(lastword ${MAKEFILE_LIST})))
ifeq (${mk},)
  mk=srcdir.mk
  mk-path=${QMK}
else
  mk-path=
endif

include ${mk-path}${mk}
